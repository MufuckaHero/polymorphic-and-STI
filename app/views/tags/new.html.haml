= simple_form_for @tag, html: { multipart: true } do |f| 
  .form-group
    = f.input :name, input_html: {class: 'form_control'}
  .form-group
    = f.input :slug, input_html: {class: 'form_control'}
  = f.button :submit, class: 'btn btn-primary' 
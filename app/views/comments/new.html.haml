.col-md-6.col-md-offset-3
  .row
    .panel.panel-default
      .panel-heading
        %h1 New Comment
      .panel-body
        = simple_form_for([@target, @comment]) do |f| 
          .form-group
            = f.input :author_name, input_html: {class: 'form_control'}
          .form-group
            = f.input :author_email, input_html: {class: 'form_control'}
          .form-group
            = f.input :content, input_html: {class: 'form_control'}
          = f.button :submit, class: "btn btn-default"
